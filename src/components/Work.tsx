import styles from "utils/styles"

const Work = () => {
	return (
		<section id="projects" className="w-full px-5 py-6 lg:px-20">
			<p className={styles["section-heading"]}>work history.</p>
		</section>
	)
}

export default Work
